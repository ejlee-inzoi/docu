# build

Overview
==

- 13 commands in this category.

Commands
==

| Command | Name | Description |
|---|---|---|
| [`build.get_customize_info`](build.get_customize_info.md) | Get Customize Info | Returns customizable parts and current material parameters of a game object (furniture, decoration,… |
| [`build.remove_object`](build.remove_object.md) | Remove Placed Object | Remove a placed game object from the current site. In build mode the removal goes through the undo/… |
| [`build.reset_material`](build.reset_material.md) | Reset Material | Reset material parameters to default. If part_index is specified, resets only that part. Otherwise … |
| [`build.scan_floors`](build.scan_floors.md) | Scan Floors | List all floors and rooms in the current site with room indices and positions. Works in both play m… |
| [`build.scan_objects`](build.scan_objects.md) | Scan Placed Objects | List game objects placed in the current site with their entity IDs, table IDs, floor levels, and lo… |
| [`build.scan_walls`](build.scan_walls.md) | Scan Walls | List all walls in the current site with edge keys and positions. Works in both play mode (character… |
| [`build.set_color`](build.set_color.md) | Set Color | Change the color of a game object. Supports hex (#FF0000) or named colors (red, blue, green, white,… |
| [`build.set_floor_color`](build.set_floor_color.md) | Set Floor Color | Change floor (or ceiling) color. Works in both play mode (character must be inside a building) and … |
| [`build.set_floor_material`](build.set_floor_material.md) | Set Floor Material | Change floor (or ceiling) material preset. If material_group/material_index are omitted, a random v… |
| [`build.set_material`](build.set_material.md) | Set Material | Set individual material parameters on a game object part. Specify only the parameters you want to c… |
| [`build.set_object_material`](build.set_object_material.md) | Set Object Material | Change a game object's material preset (texture set). If material_group/material_index are omitted,… |
| [`build.set_wall_color`](build.set_wall_color.md) | Set Wall Color | Change wall color. Works in both play mode (character must be inside a building) and build mode. Sp… |
| [`build.set_wall_material`](build.set_wall_material.md) | Set Wall Material | Change wall material preset. If material_group/material_index are omitted, a random valid preset is… |

