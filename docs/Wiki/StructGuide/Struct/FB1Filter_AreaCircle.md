## 🟢 Struct: FB1Filter_AreaCircle

Defines a circular area filter, typically used for spatial queries or gameplay areas with an optional inner exclusion zone.

### Properties

- **Radius** (`float`): Outer radius of the circular area.
- **InnerRadius** (`float`): Optional inner radius, defining a hollow area to exclude from filtering.
