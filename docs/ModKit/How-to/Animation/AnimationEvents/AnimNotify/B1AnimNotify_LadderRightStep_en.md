# B1AnimNotify_LadderRightStep

**File:** `BlueClient2/Source/BlueClient/Animation/AnimNotifies/B1AnimNotify_LadderRightStep.h / .cpp`
**Base Class:** `UAnimNotify`

> **Note:** Data extraction only — no runtime logic implemented yet (as of 240902).

## Purpose

Marker notify that flags the frame at which the right foot lands on a ladder rung. Intended to be used by the ladder traversal system to synchronize step events.
